version: "3.9"

services:
  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    command: streamlit run app.py --server.port=8501 --server.address=0.0.0.0
